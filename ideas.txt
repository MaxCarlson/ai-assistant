okay, we should also add the abikity to spin off a new ai-assistant from the input (whether quued or not). pressing ctrl+shift+enter should create a new tmux window in the current session and start a new ai-assistantusing the current input as the prompt. this way we can have multiple ai-assistants running in parallel, each with its own context and history. we can also add a command to switch between them easily, like ctrl+tab to cycle through the open ai-assistants. hmm, well thats actually different.. if we made our own tmux-like behavjir wjrh ai-assistants we could have a single window with multiple panes, each running a different ai-assistant. we could use the input field to send commands to the current pane, and use ctrl+arrow keys to switch between panes. this way we can have a more unified interface for managing multiple ai-assistants, without having to switch windows or sessions. we could also add a command to create a new pane with a new ai-assistant, like ctrl+shift+enter, and a command to close the current pane, like ctrl+w. this way we can easily manage our ai-assistants without cluttering our workspace.

also we should setup project catagories that store multiole chats and their histories.. eventually we can use these stored chats to better answerr things in tbe same project by using RAG

we shoukd also give the ai ai-assistant access to the knowledge managers projects and tasks (and we could ensure that the projects sync between tbem, as the km orojects and yasks are in a database)

also we ahoukd start adding memory.. give the ai-assistant a tool that it can uss to store memories when jt thinks its relevqnt or when told (also we xould have project levek memories)
